//=======================================// EXERCICE 1 : Refactoring - Javascript//=======================================function sumOfNumber(array) {    let sum = 0;    if (Array.isArray(array)) { // Ici = true        for (let i = 0; i < array.length; i++) {            if (typeof array[i] === "number") {                sum += array[i];            }        }    } else {        console.log("Erreur: Vous devez passer un tableau de nombres");    }    // Problème avec cette solution c'est quant ce n'était pas un tableau je n'avais pas l'erreur et la methode "length"    // ne marchait pas    // for (let i = 0; i < array.length; i++) {    //     if (Array.isArray(array) === false && typeof array[i] !== "number") {    //         console.log("Erreur: Vous devez passer un tableau de nombres");    //     } else {    //         number += array[i];    //     }    // }    return sum;}console.log(sumOfNumber([1, 2, 3, 4])); // Devrait retourner 10console.log(sumOfNumber([1, 2])); // Devrait retourner 3console.log(sumOfNumber([0])); // Devrait retourner 0console.log(sumOfNumber([1, 0])) // Devrait retourner 1console.log(sumOfNumber([1, "Haha"])) // Devrait retourner 1console.log(sumOfNumber()) // Devrait retourner "Erreur: Vous devez passer un tableau de nombres"console.log(sumOfNumber("Huhu")) // Devrait retourner "Erreur: Vous devez passer un tableau de nombres"//=======================================// Correction//=======================================function sumArray(arr) {    // Vérifier si l'entrée est un tableau    if (!Array.isArray(arr)) {        return "Erreur: Vous devez passer un tableau de nombres";    }    let sum = 0;    for (let i = 0; i < arr.length; i++) {        // Vérifier si l'élément est un nombre avant de l'ajouter        if (typeof arr[i] === 'number') {            sum += arr[i];        }    }    return sum;}console.log(sumArray([1, 2, 3, 4])); // Devrait retourner 10console.log(sumArray([1, 2])); // Devrait retourner 3console.log(sumArray([0])); // Devrait retourner 0console.log(sumArray([1, 0])) // Devrait retourner 1console.log(sumArray([1, "Haha"])) // Devrait retourner 1console.log(sumArray()) // Devrait retourner "Erreur: Vous devez passer un tableau de nombres"console.log(sumArray("Huhu")) // Devrait retourner "Erreur: Vous devez passer un tableau de nombres"console.log("========================")// function exemple(array) {//     // 1. vérifier que c'est un tableau//     if (Array.isArray(array)) {//         for (const arrayElement of array) {//             if (typeof arrayElement === "number")//                 console.log(arrayElement);//         }//     } else {//         console.log("Pas un tableau")//     }// }//// exemple([1, 2]);// exemple("string");console.log("========================");function generateUserReport(name, age, city, job, isSubscribed) {    const rapport = `Rapport utilisateur :    Nom : ${name}    Age : ${age} ans    Ville : ${city}    Profession : ${job}`;    isSubscribed(isSubscribed);    ageCategory(age);    return rapport;}function isSubscribed() {    const rapport = '';    if (isSubscribed) {        return rapport += "Statut : Abonné\n";    } else {        return rapport += "Statut : Non abonné\n";    }}function ageCategory(age) {    const rapport = '';    if (age < 18) {        return rapport += "Catégorie : Jeune\n";    } else if (age >= 18 && age < 60) {        return rapport += "Catégorie : Adulte\n";    } else {        return rapport += "Catégorie : Senior\n";    }}generateUserReport()