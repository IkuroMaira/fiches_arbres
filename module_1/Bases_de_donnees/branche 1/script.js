const fs = require('fs');const filepath = 'data.json';const content = fs.readFileSync(filepath, {encoding: 'utf8', flag: 'r'});const data = JSON.parse(content);// il y a une manière plus propre avec "await"// write your code bellow// Exercice 1 :// 1. Pour commencer, affiche dans ta console le type de `data` pour vérifier qu’il corresponde bien au type `object` et non `string`// 2. Affiche dans la console, le premier objet du tableau d’objets// 3. Affiche dans la console, le nombre d’objects contenu dans le tableau// 4. Affiche dans la console, les informations de la candidate arrivée au 4e rang// 5. Affiche dans la console, l’âge moyen des candidates// Correction de l'exercice:// console.log("content", typeof(content))// console.log("data", typeof(data))// console.log(data[0])// console.log(data.length)// const fourthCandidate = data.find(candidate => candidate.ranking === 4)// console.log(fourthCandidate)// let sumAge = 0;// for(let i=0; i<data.length; i++){//     //console.log(data[i].age)//     sumAge += data[i].age// }// console.log(sumAge/data.length)// const totalAge = data.reduce((acc, currentvalue)=> acc + currentvalue.age, 0)// console.log(totalAge)// Exercice bonus:// 1. Afficher toutes les candidates qui vivent sur Paris// 2. Afficher le pourcentage de candidates qui vivent sur Parisconsole.log("content", typeof(content));console.log("data", typeof(data));// console.log(data[0]);// console.log(data[0].city); // Lyon// console.log(data.length); // 10const displayCandidateOfParis = () => {    for (let i = 0; i < data.length; i++) {        if (data[i].city === "Paris") {            console.log(data[i].candidate);        }    }}displayCandidateOfParis();const sumCandidateOfParis = () => {    let sum = 0;    for (let i = 0; i < data.length; i++) {        if (data[i].city === "Paris") {            sum += 1;        }    }    return sum}sumCandidateOfParis()// (nombre de personne sur Paris / nombre de personne totale) * 100const calculatePercentage = () => {    const resultPercentage = (sumCandidateOfParis() / data.length) * 100;    console.log(resultPercentage, '%');}calculatePercentage();